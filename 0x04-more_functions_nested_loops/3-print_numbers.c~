#include "main.h"
/**
 * print_numbers - function name
 * this function  prints the numbers, from 0 to 9
 * Return: 0;
 */
void print_numbers(void)
{
int i;
for (i = 0; i < 10; i++)
_putchar(i);
_putchar('\n');
}
