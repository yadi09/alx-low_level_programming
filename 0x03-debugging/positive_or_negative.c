#include "main.h"
/**
 * positive_or_negative - is return void
 *
 * @i: parameter
 * this function creates to test the 0-main.c file
 *
 * Return: 0;
 */
void positive_or_negative(int i)
{
if (i == 0)
printf("%d is zero\n", i);
else if (i < 0)
printf("%d is negative\n", i);
else
printf("%d is positive\n", i);
return;
}
