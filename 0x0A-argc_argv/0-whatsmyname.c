#include <stdio.h>

/**
 * main - the main function
 * @argc: number of vector
 * @argv: vector
 * Return: 0;
 **/

int main(int __attribute__((unused)) argc, char **argv)
{
printf("%s\n", argv[0]);
return (0);
}
