#include "main.h"
/**
 * _abs - this function return int value
 *
 * @n: this parameter hold int value
 *
 * this function computes the absolute value of an integer.
 *
 * Return: 0;
 */
int _abs(int n)
{
if (n < 0)
return (-1 * n);
else
return (n);
}
